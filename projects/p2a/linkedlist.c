#include "linkedlist.h"
#include <stddef.h>

struct lst *ll_init() {
  return NULL;
}

void ll_push_back(struct lst *ll, void *value) {}

void *ll_remove(struct lst *ll, struct lst_node *node) { return NULL; }